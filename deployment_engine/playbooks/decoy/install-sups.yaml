---
# Ansible Playbook: install-sups.yaml
# Install SUP agents on provisioned VMs (stage 1 → reboot → stage 2).
# Display/summary is handled by the Python CLI.
#
# Prerequisites: provision-vms.yaml must have run first (inventory.ini exists).
#
# Usage:
#   ansible-playbook -i runs/032226/inventory.ini install-sups.yaml \
#       -e deployment_dir=../decoy-controls

- name: Install SUPs
  hosts: sup_hosts
  become: yes
  serial: 10
  strategy: free
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ServerAliveInterval=30 -o ServerAliveCountMax=60 -o ConnectTimeout=30 -o IdentitiesOnly=yes'
    ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
    ansible_ssh_retries: 5
    deployment_dir: "{{ playbook_dir }}"
    config_file: "{{ deployment_dir }}/config.yaml"
    ruse_repo: "https://github.com/LampSteven17/RUSE.git"
    ruse_revision: ""
    workflow_gpu_tier: "v100"
    probe_install: false
    probe_started_at: ""
    staged_probe_plans: "/tmp/ruse-probe-plans"
    ruse_path: "/opt/ruse"
    canonical_workflow_configs:
      - scripted-cpu
      - mchp-cpu
      - browseruse-gpu
      - smolagents-gpu
    canonical_plan_filenames:
      scripted-cpu: scripted-v1.json
      mchp-cpu: mchp-v1.json
      browseruse-gpu: browseruse-v1.json
      smolagents-gpu: smolagents-v1.json
    staged_workflow_plan: "/tmp/ruse-{{ sup_behavior }}-plan.json"

  pre_tasks:
    - name: Wait for SSH
      wait_for_connection:
        delay: 5
        timeout: 300
      retries: 3
      delay: 10

    - name: Load deployment configuration
      include_vars:
        file: "{{ config_file }}"
      delegate_to: localhost
      run_once: true

    - name: Require immutable RUSE revision
      assert:
        that:
          - ruse_revision is match('^[0-9a-f]{40}$')
        fail_msg: >-
          ruse_revision must be a full 40-character Git commit SHA. The DECOY
          orchestrator must resolve and pass it before provisioning.
      run_once: true

  tasks:
    # C0 is bare Ubuntu — skip everything, just verify SSH works
    - name: Skip C0 (bare control VM)
      meta: end_host
      when: sup_behavior == 'C0'

    - name: Wait for cloud-init
      command: cloud-init status --wait
      changed_when: false
      timeout: 300

    # Pin VM clock to EST/EDT. PHASE-emitted timing arrays (hourly_distribution,
    # activity_probability_per_hour, hourly_std_targets) are hour-of-day indexed
    # off EST Zeek captures. A UTC-zoned SUP would index the wrong slot every
    # hour — behavior fires 4-5h offset from the measurement window.
    - name: Set timezone to America/New_York
      timezone:
        name: America/New_York

    - name: Stop and mask automatic APT activity for canonical SUPs
      when: sup_behavior in canonical_workflow_configs
      command:
        argv:
          - systemctl
          - mask
          - --now
          - apt-daily.service
          - apt-daily.timer
          - apt-daily-upgrade.service
          - apt-daily-upgrade.timer
          - unattended-upgrades.service

    # Retry the early fetch tasks — apt mirrors and github.com both flake
    # with "unknown reason" on fresh VMs. These retries don't mask failure:
    # after N attempts the task still fails loud and spinup.py aborts.
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
      register: apt_cache_update
      until: apt_cache_update is succeeded
      retries: 3
      delay: 30

    - name: Install prerequisites
      apt:
        name: [git, curl, wget]
        state: present
      register: apt_prereqs
      until: apt_prereqs is succeeded
      retries: 3
      delay: 30

    - name: Clone RUSE repo
      git:
        repo: "{{ ruse_repo }}"
        dest: "{{ ruse_path }}"
        version: "{{ ruse_revision }}"
        force: yes
      register: clone_ruse
      until: clone_ruse is succeeded
      retries: 3
      delay: 15

    - name: Read installed RUSE revision
      command: git -C {{ ruse_path }} rev-parse HEAD
      register: installed_ruse_revision
      changed_when: false

    - name: Verify installed RUSE revision
      assert:
        that:
          - installed_ruse_revision.stdout | trim == ruse_revision
        fail_msg: >-
          RUSE revision mismatch on {{ inventory_hostname }}: requested
          {{ ruse_revision }}, installed {{ installed_ruse_revision.stdout | trim }}.

    - name: Record installed RUSE revision
      copy:
        content: "{{ ruse_revision }}\n"
        dest: "{{ ruse_path }}/DEPLOYED_RUSE_REVISION"
        mode: '0644'

    - name: Make INSTALL_SUP.sh executable
      file:
        path: "{{ ruse_path }}/INSTALL_SUP.sh"
        mode: '0755'

    - name: "Stage 1: system deps + drivers"
      shell: |
        cd {{ ruse_path }}
        RUSE_WORKFLOW_GPU_TIER="{{ workflow_gpu_tier }}" \
          ./INSTALL_SUP.sh --{{ sup_behavior }} --stage=1
      args:
        executable: /bin/bash
      async: 1800
      poll: 30
      register: stage1_result
      failed_when: stage1_result.rc not in [0, 100]

    - name: Reboot for NVIDIA drivers (exit code 100)
      reboot:
        reboot_timeout: 600
        msg: "Rebooting for NVIDIA driver"
      when: stage1_result.rc == 100

    - name: Stage assigned canonical workflow plan
      when: sup_behavior in canonical_workflow_configs and not probe_install
      copy:
        src: "{{ behavior_source }}/{{ canonical_plan_filenames[sup_behavior] }}"
        dest: "{{ staged_workflow_plan }}"
        mode: '0644'

    - name: Stage approved probe daily variants
      when: probe_install and probe_workflow != 'idle'
      copy:
        src: "{{ behavior_source }}/"
        dest: "{{ staged_probe_plans }}/"
        mode: '0644'

    - name: "Stage 2: ollama + python + services"
      shell: |
        cd {{ ruse_path }}
        RUSE_NO_SERVICE_START=1 \
        RUSE_WORKFLOW_BEHAVIOR_PATH="{{ staged_workflow_plan if sup_behavior in canonical_workflow_configs else '' }}" \
        RUSE_WORKFLOW_GPU_TIER="{{ workflow_gpu_tier }}" \
        RUSE_DEPLOYMENT_TYPE="{{ 'probe' if probe_install else 'decoy' }}" \
        RUSE_PROBE_WORKFLOW="{{ probe_workflow | default('') if probe_install else '' }}" \
        RUSE_PROBE_STARTED_AT="{{ probe_started_at }}" \
        RUSE_PROBE_PLANS_DIR="{{ staged_probe_plans if probe_install else '' }}" \
          ./INSTALL_SUP.sh --{{ sup_behavior }} --stage=2
      args:
        executable: /bin/bash
      async: 1800
      poll: 30
      register: stage2_result
      # RUSE_NO_SERVICE_START=1: install + enable, do NOT start. The mandatory
      # behavior.json check crash-loops the service 60-100x between INSTALL_SUP.sh
      # finishing and distribute-behavior-configs.yaml landing the file — those
      # restarts inflate NRestarts permanently and pollute the audit forever.
      # distribute-behavior-configs.yaml starts the service AFTER the file is in
      # place. M0 (which skips distribute and is expected to crash on Linux) gets
      # started at the tail of THIS playbook.

    - name: Remove staged canonical workflow plan
      when: sup_behavior in canonical_workflow_configs and not probe_install
      file:
        path: "{{ staged_workflow_plan }}"
        state: absent

    - name: Remove staged probe daily variants
      when: probe_install and probe_workflow != 'idle'
      file:
        path: "{{ staged_probe_plans }}"
        state: absent

    - name: Assert Stage 2 succeeded
      # S3: Previously stage2_result.rc was registered but never explicitly
      # checked. Ansible defaults to failing the task on rc != 0 so the play
      # would abort, but with no clear message. Add explicit assertion so the
      # failure reason reaches the operator.
      fail:
        msg: >-
          Stage 2 install failed on {{ inventory_hostname }} (rc={{ stage2_result.rc }}).
          stderr: {{ stage2_result.stderr | default('') | truncate(400) }}
      when: stage2_result.rc != 0

    - name: Derive canonical workflow service name
      when: sup_behavior in canonical_workflow_configs
      set_fact:
        canonical_sup_service: "{{ sup_behavior | lower | replace('.', '_') }}.service"

    - name: Verify automatic APT activity is disabled before service startup
      when: sup_behavior in canonical_workflow_configs
      shell: |
        set -euo pipefail
        automatic_apt_unit_is_safe() {
          local enabled_state="$1"
          local active_state="$2"
          [[ "$enabled_state" == masked && \
            ( "$active_state" == inactive || "$active_state" == failed ) ]]
        }
        for unit in \
          apt-daily.service \
          apt-daily.timer \
          apt-daily-upgrade.service \
          apt-daily-upgrade.timer \
          unattended-upgrades.service
        do
          enabled_state=$(systemctl is-enabled "$unit" 2>/dev/null || true)
          active_state=$(systemctl is-active "$unit" 2>/dev/null || true)
          if ! automatic_apt_unit_is_safe "$enabled_state" "$active_state"; then
            echo "$unit is not safely disabled: enabled=$enabled_state active=$active_state" >&2
            exit 1
          fi
        done
      args:
        executable: /bin/bash
      changed_when: false

    - name: Start canonical workflow service after Stage 2
      when: sup_behavior in canonical_workflow_configs
      systemd:
        name: "{{ canonical_sup_service }}"
        state: started
        daemon_reload: yes

    - name: Wait for canonical workflow service to become active
      when: sup_behavior in canonical_workflow_configs
      command: "systemctl is-active {{ canonical_sup_service }}"
      register: canonical_service_probe
      changed_when: false
      failed_when: false
      retries: 12
      delay: 5
      until: canonical_service_probe.stdout | trim == 'active'

    - name: Assert canonical workflow service is active
      when:
        - sup_behavior in canonical_workflow_configs
        - canonical_service_probe.stdout | default('') | trim != 'active'
      fail:
        msg: >-
          {{ canonical_sup_service }} did not become active after Stage 2 on
          {{ inventory_hostname }}. Check systemctl status and the installed
          workflow plan under /opt/ruse/deployed_sups/{{ sup_behavior }}.

    # Legacy service-active assertions remain at the tail of
    # distribute-behavior-configs.yaml. Those services cannot start until the
    # separately distributed behavior.json lands. Canonical workflow controls
    # are asserted above because INSTALL_SUP.sh installs their plan itself.

    # MCHP brains have a slow Selenium/pyautogui memory leak. Schedule a daily
    # service restart at 03:00 UTC + a weekly full VM reboot Sundays at 04:00 UTC
    # to keep them healthy. M brains only — B/S brains don't need this.
    - name: Install MCHP maintenance cron entries (daily restart + weekly reboot)
      when: sup_behavior is match('^M[0-9]')
      cron:
        name: "{{ item.name }}"
        user: root
        minute: "{{ item.minute }}"
        hour: "{{ item.hour }}"
        weekday: "{{ item.weekday | default('*') }}"
        job: "{{ item.job }}"
      loop:
        - name: mchp-daily-restart
          minute: "0"
          hour: "3"
          job: "/bin/systemctl restart {{ sup_behavior | lower | replace('.', '_') }}.service"
        - name: mchp-weekly-reboot
          minute: "0"
          hour: "4"
          weekday: "0"
          job: "/sbin/reboot"

    - name: Verify MCHP cron entries were installed (M-series only)
      # S5: Previously the cron module's success wasn't audited. If the module
      # silently no-op'd (missing crontab, permission issue), the VM would hit
      # the Selenium memory leak after ~4 days with no warning. This check
      # confirms both cron entries actually reached the crontab.
      when: sup_behavior is match('^M[0-9]')
      shell: crontab -l | grep -cE 'mchp-(daily-restart|weekly-reboot)' || echo 0
      register: cron_count
      changed_when: false
      failed_when: false

    - name: Assert both MCHP crons are installed
      fail:
        msg: >-
          Only {{ cron_count.stdout | trim }}/2 MCHP maintenance crons found on
          {{ inventory_hostname }}. Without these the M-brain will exhaust memory
          after ~4 days. Check crontab -l and systemctl status cron.
      when:
        - sup_behavior is match('^M[0-9]')
        - (cron_count.stdout | default('0') | trim | int) < 2

    # M0 has no behavior.json (read-only upstream control) and skips
    # distribute-behavior-configs.yaml entirely. Start it here, since the
    # stage-2 install left every service stopped (RUSE_NO_SERVICE_START=1).
    # M0 is expected to crash-loop on Linux (Windows-only os.startfile()) — the
    # audit reports `EXPECTED (M0 upstream crashes on Linux)` and treats it as
    # the baseline. C0 has no service installed at all, so it needs no start.
    - name: Start M0 service (skips distribute, no behavior.json required)
      when: sup_behavior == 'M0'
      systemd:
        name: m0.service
        state: started
